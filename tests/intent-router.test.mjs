import test from 'node:test';import assert from 'node:assert/strict';import {classifyAnimationIntent,executionProof} from '../automation/intent-router.mjs';
test('supported walk-cycle and follow-up intents require SPRITED',()=>{assert.equal(classifyAnimationIntent('Create an 8-frame walk-cycle spritesheet').requires_sprited,true);assert.equal(classifyAnimationIntent('Fix frame 5',{active_animation_id:'a1'}).follow_up,true);});
test('generic request is not falsely labeled SPRITED',()=>{const intent=classifyAnimationIntent('make a landscape image');assert.equal(intent.requires_sprited,false);const proof=executionProof({});assert.equal(proof.result_status,'UNVERIFIED_RESULT');assert.equal(proof.sprited_pipeline_active,false);});
test('execution proof exposes real attempt evidence only',()=>{const proof=executionProof({attempt_id:'a1',strategy:'TARGETED_FRAME_REPAIR',subsystems:['Preflight','Motion Lock'],status:'NEEDS_REPAIR',debug_snapshot:{attempt_id:'a1'}});assert.equal(proof.sprited_pipeline_active,true);assert.equal(proof.attempt_id,'a1');assert.deepEqual(proof.subsystems_invoked,['Preflight','Motion Lock']);});
